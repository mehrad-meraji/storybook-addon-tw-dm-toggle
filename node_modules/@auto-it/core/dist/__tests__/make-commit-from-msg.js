"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/** Construct a dummy commit for testing. */
const makeCommitFromMsg = (subject, options = {}) => ({
    hash: options.hash || "foo",
    labels: options.labels || [],
    authorName: options.name !== undefined && options.name !== null
        ? options.name
        : "Adam Dierkens",
    authorEmail: options.email || "adam@dierkens.com",
    files: options.files || [],
    authors: [
        Object.assign({ type: options.type, name: options.name !== undefined && options.name !== null
                ? options.name
                : "Adam Dierkens", email: options.email || "adam@dierkens.com" }, (options.username ? { username: options.username } : {})),
    ],
    subject,
    pullRequest: options.pullRequest,
});
exports.default = makeCommitFromMsg;
//# sourceMappingURL=make-commit-from-msg.js.map