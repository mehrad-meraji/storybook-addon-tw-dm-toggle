"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const child_process_1 = require("child_process");
/**
 *
 */
function verifyAuth(remote, branch) {
    return new Promise((resolve) => {
        let timeout = null;
        /** Clear the timeout timeout */
        const clear = () => {
            if (timeout) {
                clearTimeout(timeout);
            }
        };
        try {
            const child = child_process_1.spawn(`git push --dry-run --no-verify ${remote} HEAD:${branch} -q`, {
                cwd: process.cwd(),
                env: process.env,
                detached: true,
                shell: true,
            });
            timeout = setTimeout(() => {
                // Kill the spawned process and it's children
                process.kill(-child.pid);
                resolve(false);
            }, 5 * 1000);
            let err = "";
            child.stderr.on("data", (data) => {
                err += data.toString();
            });
            child.on("exit", () => {
                clear();
                resolve(!err.startsWith("fatal: could not read Username") &&
                    !err.startsWith("ssh_askpass") &&
                    !err.includes("fatal"));
            });
        }
        catch (error) {
            clear();
            resolve(false);
        }
    });
}
exports.default = verifyAuth;
//# sourceMappingURL=verify-auth.js.map